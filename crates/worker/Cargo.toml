[package]
name = "worker"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
tonic = { workspace = true }
tokio = { workspace = true, features = ["full"] }
tokio-stream = { workspace = true }
# switch to llvm?
wasmer = { version = "7.0.1", features = ["cranelift"]}
wasmer-compiler-cranelift = { version = "7.0.1" }
wasmer-wasix = { version = "0.700.1" }
wasmer-types = { version = "7.0.1" }
thiserror = { workspace = true }
clap = { workspace = true }
