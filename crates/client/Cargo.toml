[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
tonic = { workspace = true }
tokio = { workspace = true, features = ["full"] }
clap = { version = "4.6.0", features = ["derive"]}
clap_derive = { version = "4.6.0" }
