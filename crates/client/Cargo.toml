[package]
name = "mini-lambda-client"
version = "0.1.0"
edition = "2024"

[dependencies]
mini-lambda-shared = { path = "../shared" }
tonic = { workspace = true }
tokio = { workspace = true, features = ["full"] }
